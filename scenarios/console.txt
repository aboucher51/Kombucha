# Console commands run as scenario lines — same vocabulary, one seam.
wait 5
volume Music 80
binds
save scenario_probe
saves
load scenario_probe
delete_save scenario_probe
expect_fail load scenario_probe
expect_fail frobnicate
# Quotes are ordinary characters and reach the handler: a console command
# that takes JSON depends on it. Only the token `""` is the empty value.
note "Shore War"
assert_note "Shore War"
shot console-done
